export function handleNull(caseName: string, CaseModel: any, url: string) {
    const caseInfo = {
        caseName: caseName,
        CaseModel: CaseModel,
        url: url,
    }

    return caseInfo;
}